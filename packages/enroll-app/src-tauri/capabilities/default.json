{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window. Only the core defaults are granted; the shell plugin is intentionally NOT enabled — process spawning happens inside the Rust commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
